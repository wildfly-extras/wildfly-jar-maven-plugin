plugins {
    id 'java'
    id 'war'
}

repositories {    
    mavenCentral()
    mavenLocal()
}

configurations{
    kafkaRAR {
        transitive = false
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

war{
    archiveBaseName = "gradle-demo"
    exclude("*.cli")
    exclude("*properties")
    exclude("**module.xml")
}

dependencies {
    compileOnly 'jakarta.platform:jakarta.jakartaee-api:10.0.0'
<<<<<<< HEAD
    amqRAR 'org.apache.activemq:activemq-rar:5.16.0'
=======
    compileOnly 'fish.payara.cloud.connectors.kafka:kafka-jca-api:0.9.0-SNAPSHOT'
    compileOnly 'org.jboss.ejb3:jboss-ejb3-ext-api:2.3.0.Final'
    kafkaRAR 'fish.payara.cloud.connectors.kafka:kafka-rar:0.9.0-SNAPSHOT'
>>>>>>> 345ad71 (Moving form Activemq to Kafka for the RAR demo)
}


task getRAR(type: Copy) {
    from configurations.kafkaRAR
    into layout.buildDirectory.dir("../target/build")
}

task makeHollowJar(type: Exec){
    dependsOn getRAR
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'mvnw.cmd', 'wildfly-jar:package'
    } else {
        commandLine './mvnw', 'wildfly-jar:package'
    }
}

task mavenClean(type: Exec){
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'mvnw.cmd', 'clean'
    } else {
        commandLine './mvnw', 'clean'
    }
}
wrapper{
    gradleVersion = '7.5'
}


clean.dependsOn mavenClean
build.dependsOn makeHollowJar

